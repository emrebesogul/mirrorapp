import React, {Component} from "react";

import {
    View,
    Text
} from 'react-native';

class FakePage extends Component {

    render() {
        return (
            <View>
                <Text>Should render QR-Code scanning page now...</Text>
            </View>
        );
    }
}

export default FakePage;
